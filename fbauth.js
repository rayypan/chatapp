import { Auth } from 'fbinit.js';
import { rtdb } from 'fbrtdb.js';

const authenticate = function(email, name) {
    
}

const isAuthenticated = function() {
    
}

const getName = function() {
    
}

export const auth = {
    authenticate,
    isAuthenticated,
    getName,
};
